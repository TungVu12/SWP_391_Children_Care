@model IEnumerable<Reservation>

@{
    ViewData["Title"] = "Index";
}

@if (TempData["ResultOk"] != null)
{
    <h1 class="alert-success">@TempData["ResultOk"]</h1>
}

<div class="container shadow p-5">

@*
    <div class="col mb-3">
        <a asp-controller="Children" asp-action="Create" class="btn btn-lg btn-primary"><i class="bi bi-file-plus-fill"></i>Thêm mới bé</a>
    </div>*@
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th scope="col">Tiêu đề buổi hẹn khám</th>
                <th scope="col">Nội dung khám bệnh</th>
                <th scope="col">Ngày tạo lịch</th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td width="20%">
                        @item.r_title
                    </td>
                    <td width="25%">
                        @item.r_content
                    </td>
                    <td width="20%">
                        @item.create_date
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>